pytest-homeassistant-custom-component>=0.13.190
pytest>=8.0
pytest-asyncio>=0.23
pycryptodome>=3.20
ruff>=0.6
# Third-party deps pulled in when the config entry sets up under pytest: ffmpeg -> ha-ffmpeg
# (the camera setup calls get_ffmpeg_manager); the `camera` component imports turbojpeg (-> PyTurboJPEG)
# and av at module load. Import-only here (no native libjpeg-turbo/ffmpeg binary needed by the tests).
ha-ffmpeg
av
PyTurboJPEG